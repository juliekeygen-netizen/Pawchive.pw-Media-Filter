'use strict';
const assert=require('node:assert/strict');const{loadUserscript}=require('./test-helper.cjs');
(async()=>{const{api,originalSource}=loadUserscript(),{Config,Lifecycle,PopularJobManager}=api;assert.equal(Config.version,'0.13.3');assert.match(originalSource,/\/\/ @version\s+0\.13\.3/);assert.match(Lifecycle.installHistorySignals.toString(),/history\.pushState=function/);assert.match(Lifecycle.installHistorySignals.toString(),/history\.replaceState=function/);assert.match(Lifecycle.start.toString(),/installHistorySignals\(signal\)/);const original=Lifecycle.ensureMounted;let calls=0;Lifecycle.stopped=false;Lifecycle.microtaskQueued=false;Lifecycle.ensureMounted=()=>{calls+=1;return Promise.resolve();};Lifecycle.schedule('spam-1');Lifecycle.schedule('spam-2');Lifecycle.schedule('spam-3');await Promise.resolve();assert.equal(calls,1);Lifecycle.ensureMounted=original;let aborted=0;PopularJobManager.paused=false;PopularJobManager.activeJob={periodKey:'x',context:{periodKey:'x'},status:'running',controller:{abort(){aborted+=1;}}};PopularJobManager.suspendForBfcache();assert.equal(PopularJobManager.paused,true);assert.equal(PopularJobManager.activeJob.interruptedByNavigation,true);assert.equal(aborted,1);PopularJobManager.pendingJobs=[];PopularJobManager.activeJob=null;PopularJobManager.resumeFromBfcache();assert.equal(PopularJobManager.paused,false);assert.match(Lifecycle.prepareSnapshot.toString(),/mountController\?\.abort\(\)/);assert.match(Lifecycle.prepareSnapshot.toString(),/PopularJobManager\.suspendForBfcache/);assert.match(Lifecycle.handlePageShow.toString(),/PopularJobManager\.resumeFromBfcache/);assert.match(Lifecycle.ensureMounted.toString(),/currentKey===nextKey&&Lifecycle\.healthy\(current\)/);console.log('Pawchive Media Filter v0.13.3 navigation lifecycle audit tests passed.');})();
